---
- name: Template docker-compose.yml
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: '{{ airplex_install_dir }}/docker-compose.yml'
    owner: root
    group: root
    mode: '0644'
  notify: restart airplex
